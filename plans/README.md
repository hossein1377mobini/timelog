# Implementation Plans — timelog (compass-next)

Generated by `/improve quick` on 2026-07-11. Execute in the order below unless dependencies say otherwise.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Harden JWT secret with production env guard | P1 | S | — | TODO |
| 002 | Add integration tests for auth & CRUD paths | P1 | L | — | TODO |
| 003 | Eliminate dual storage layer (remove localStorage) | P2 | M | 002 | TODO |
| 004 | Add zod input schema validation on all mutable endpoints | P2 | M | — | TODO |

## Dependency notes

- **003** (remove localStorage) should ideally wait for **002** (integration tests) — characterization tests before the refactor give you a safety net. Can be done manually if you're confident in coverage.

## Findings considered and rejected

- Default DB password `'123456789'` in `lib/db.ts:8`: dev-only, same pattern as SEC-01, combined mitigation.
- `parseInt(queryParam, 10)` without NaN guard: PG catches via type error. Would be resolved by zod validation in plan 004.
- Unused `prisma` devDependency: minor bloat, not high leverage.
- CSP commented out: S effort, non-dependent — rolled into 001 as a bonus step.
